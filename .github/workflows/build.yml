name: build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore ./DocSearch/DocSearch.sln

    - name: Build
      run: dotnet build ./DocSearch/DocSearch.sln --no-restore -c Release

    - name: Test
      run: dotnet test ./DocSearch/DocSearch.sln --no-build -c Release
